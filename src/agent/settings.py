# ====================== 关键词配置 ======================
# 故障排查关键词
TROUBLE_KEYWORDS = ["卡", "慢", "宕机", "500", "502", "磁盘满", "cpu高", "连不上"]
# 问答关键词
QA_KEYWORDS = ["端口", "密码", "命令", "配置", "怎么", "如何", "多少"]

# ====================== Mock RAG 知识库 ======================
MOCK_OPS_KNOWLEDGE = {
    "nginx端口": "Nginx默认端口：80（HTTP）、443（HTTPS），配置文件路径：/etc/nginx/nginx.conf",
    "mysql默认密码": "MySQL默认无密码，初始化密码存放在 /var/log/mysqld.log",
    "redis端口": "Redis默认端口：6379，配置文件：redis.conf",
    "服务器重启命令": "Linux重启命令：reboot（需root权限）"
}

# ====================== 故障排查默认计划 ======================
DEFAULT_TROUBLESHOOT_PLAN = [
    "检查CPU使用率",
    "检查磁盘使用率",
    "检查应用错误日志",
    "根因定位与诊断"
]