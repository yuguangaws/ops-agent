# To separate your traces from other application
LANGSMITH_PROJECT=new-agent

# Add API keys for connecting to LLM providers, data sources, and other integrations here
ZHIPUAI_API_KEY=xxxxxxxxxxxx
ZHIPUAI_DEFAULT_MODEL=glm-3-turbo

# MySQL 连接配置（mcp_server/tools/db.py 的 check_db 工具使用，默认值仅用于本地开发）
OPS_MYSQL_HOST=localhost
OPS_MYSQL_PORT=3306
OPS_MYSQL_USER=root
OPS_MYSQL_PASSWORD=xxxxxxxxxxxx
OPS_MYSQL_DATABASE=bi_test

# LangGraph Checkpoint 持久化配置（Postgres，用于 human_approval 中断/恢复）
# 本地用 docker compose up -d 启动，见 docker-compose.yml
OPS_CHECKPOINT_PG_HOST=localhost
OPS_CHECKPOINT_PG_PORT=5442
OPS_CHECKPOINT_PG_USER=ops_agent
OPS_CHECKPOINT_PG_PASSWORD=ops_agent
OPS_CHECKPOINT_PG_DATABASE=ops_agent_checkpoints
